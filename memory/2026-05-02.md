# Memory — 2026-05-02

## UnfoldAI Token Calculator Upgrade
- Upgraded `/tools/token-calculator` with 3 tabs: Budget Planner, Token Counter, ROI Calculator
- File lives at `/data/.openclaw/workspace/unfoldai/src/app/tools/token-calculator/page.tsx`
- Build passes locally (next build exit 0)

## Deployment Issues (ongoing as of 11am)
- GitHub repo has TWO copies of the app: root `src/` and nested `unfoldai/src/` — caused confusion
- Vercel root directory was set to `unfoldai` initially, later changed to `.` (root)
- Multiple pushes made to get correct files at root: layout.tsx, components, config files
- Submodule warning appeared: "Failed to fetch one or more git submodules" — may be causing Vercel issues
- Calculator still not live as of ~11am — Hashim attempting manual redeploy with cache clear

## Claude Sonnet via OpenRouter
- Added `openrouter/anthropic/claude-sonnet-4-5` as a model option
- Currently running on this model in this session

## Heartbeats
- Disabled heartbeats at Hashim's request (were burning API tokens every 30 min with empty HEARTBEAT.md)
